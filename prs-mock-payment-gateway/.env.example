# Payment Gateway environment variables
# Copy to .env.local for local development (never commit .env.local)

# URL of the Payment Service (empty = use Vite proxy in dev, nginx proxy in Docker)
VITE_PAYMENT_SERVICE_URL=

# URL of the main frontend returned to after payment result
VITE_FRONTEND_URL=http://localhost:3000
